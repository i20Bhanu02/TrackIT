<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <meta http-equiv="refresh" content="10">
  <title>Secrets</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.3/css/all.css" integrity="sha384-UHRtZLI+pbxtHCWp1t77Bi1L4ZtiqrqD80Kn4Z8NTSRyMA2Fd33n5dQ8lWUE00s/" crossorigin="anonymous">
  <link rel="stylesheet" href="css/styles.css">
</head>

<body>

<div class="jumbotron">
  <div class="container">
    <h1 class="display-3">Wishlist</h1>
    
    <table>
        <thead>
          <th>#</th>
          <th>Coin</th>
          <th>Price</th>
          <th>Market Cap</th>
          <th>Volume 24h</th>
          <th>All-time High</th>
          <th>24h</th>
        </thead>

        <tbody>
          <% for(var i=0;i<coins.length;i++){ %>
            <tr>
              <td><%= i+1  %></td>
              <td><p><img src=<%= coins[i].png64 %>> <%=coins[i].name%> <%=coins[i].code%></p></td>
              <td><%=sign%><%=Math.round((coins[i].rate + Number.EPSILON) * 100) / 100%></td>
              <td><%=sign%><%=Math.round((coins[i].cap + Number.EPSILON) * 100) / 100 %></td>
              <td><%=sign%><%=Math.round((coins[i].volume + Number.EPSILON) * 100) / 100 %></td>
              <td>$<%=Math.round((coins[i].allTimeHighUSD + Number.EPSILON) * 100) / 100 %></td>
              <td><%=Math.round(((coins[i].delta.day-1)*100 + Number.EPSILON) * 100) / 100 %>%</td>
            </tr>
          <% } %>
        </tbody>
      </table>

  </div>
</div>

<script src="JS/dash.js"></script>
    
<%- include('partials/footer') %>